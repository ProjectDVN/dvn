module zid.components;

public
{
  import zid.components.animation;
  import zid.components.button;
  import zid.components.image;
  import zid.components.label;
  import zid.components.panel;
  import zid.components.scrollbar;
  import zid.components.textbox;
  import zid.components.dropdown;
  import zid.components.checkbox;
  // import zid.components.button;
    // Allow to be an imageset of default,hover,click
    // But also just a set of rectangles default,hover,click
    // Should also allow imageicons and text!
  // import zid.components.grouppanel;
    // Should draw a border around the panel
    // Then at the top display a label for the panel name
    // Which should hide part of the border
    // Components are drawn with a scrolloffset in the group panel
    // Although it doesn't allow natural scrolling!
  // import zid.components.scrollbars;
    // Basic scroll bar that is eitehr horizontal or vertical
    // Allows the buttons to have same properties as the components.button
    // Same with the scrollbar itself ^
    // It defaults to a simple scroll bar that is just a "line"
  // import zid.components.progressbar;
    // A simple progressbar that allows use an image for stretching
    // Or simply rectangles for styling
    // Allows border from the rectangles etc.
  // import zid.components.textfield;
    // A basic textfield that allows to be styled using rectangles OR images ...
  // import zid.components.numberfield;
  // import zid.components.dropdown;
  // import zid.components.checkbox;
  // import zid.components.radiobutton;
  // import zid.components.togglebutton;
  // import zid.components.imageanimation;
  // import zid.components.slide;
  // import zid.components.multilinetextbox;
  // import zid.components.grid;
  // import zid.components.treeview;
  // import zid.components.tabview;
  // import zid.components.listview;
  // import zid.components.menustrip;
  // import zid.components.toolstrip;
  // import zid.components.statusbar;
  // import zid.components.propertiespanel;
}
